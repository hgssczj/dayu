position: both
pod-template:
  image: exposure-identification
  imagePullPolicy: IfNotPresent
  env:
    - name: PROCESSOR_NAME
      value: "roi_classifier_processor"
    - name: ROI_CLASSIFIER_PARAMETERS
      value: >
        {
          'trt_weights': 'resnet50.engine',
          'non_trt_weights':'resnet50.pth',
          'device':0
        }
    - name: SCENARIOS_EXTRACTORS
      value: >
        [ 'obj_num']
    - name: PRO_QUEUE_NAME
      value: "simple"
    - name: USE_TENSORRT
      value: "False"
port-open:
  pos: both
  port: 9000
file-mount:
  - pos: both
    path: "processor/exposure-identification/"
